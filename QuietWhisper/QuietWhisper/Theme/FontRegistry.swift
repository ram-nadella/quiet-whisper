// FontRegistry.swift
// Currently a no-op — Quiet Whisper uses system fonts (New York / SF Pro / SF Mono).
// If we later bundle Fraunces / Inter / JetBrains Mono, register them here.

import Foundation

enum FontRegistry {
    static func registerBundledFonts() {}
}
